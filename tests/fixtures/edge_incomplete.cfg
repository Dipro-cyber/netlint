! edge_incomplete.cfg
! Pathological/incomplete configurations:
!   - Interface block with no sub-commands at all
!   - Interface with only a description, no IP
!   - ip route with only network/mask, no next-hop (malformed)
!   - access-list line with no action
!   - vlan block with no name
!   - line vty with no transport or login statement
!   - hostname missing entirely
!   - Trailing whitespace and blank lines

!
!
interface GigabitEthernet0/0
!
interface GigabitEthernet0/1
 description This interface has no IP configured
!
interface GigabitEthernet0/2
 ip address 10.0.0.1 255.255.255.0
!
vlan 10
!
vlan 20
 name DEFINED
!
! Malformed route — missing next-hop (parser should warn, not crash)
ip route 10.10.0.0 255.255.0.0
!
! ACL with no entries
ip access-list extended EMPTY_ACL
!
! VTY with nothing configured
line vty 0 4
!
end

